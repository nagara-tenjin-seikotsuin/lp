---
import Layout from "@/layouts/Layout.astro";

export const prerender = false;

const locals = Astro.locals;
const variant = locals.ab;
---

<Layout>
  <pre>{JSON.stringify(locals, null, 2)}</pre>
  <p>ABテストのバリアント: {variant}</p>
</Layout>
